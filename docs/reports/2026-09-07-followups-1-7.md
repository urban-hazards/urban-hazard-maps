# Follow-ups 1–7 — 2026-09-07

All seven items are implemented in the working tree and passed the required
pipeline and frontend checks at the end of each item. Continued from the
existing partial commit `ae5ed97`; its sampler and encampment finding were
retained, reviewed and completed.

**Git commits are blocked.** This environment explicitly makes the checkout's
`.git` read-only. Even `git add -n` failed creating `.git/index.lock` with
`Operation not permitted`. No new commits were created and nothing was pushed.
One sequential patch per item is preserved in
`/private/tmp/boston-followups-20260907/01.patch` through `07.patch`; these apply
after `ae5ed97`. Patch 07 also contains this report. Proposed commit messages
are listed below. The changes are already present here; do not apply the patches
again to this working tree. The per-item commit requirement remains outstanding.

Neither the production pipeline nor the scraper was run. `pipeline/.env` was
not read. No actual precision sample or production CSV backfill was performed.
Tests use moto and mocked urllib; the inherited test fixtures disable dotenv
and live HTTP. No LLM calls were added to the daily path or the research tool.

## Verification

At the end of every item:

```sh
cd pipeline
uv run ruff format src/ tests/
uv run ruff check src/ tests/
uv run mypy src/
uv run pytest -q
```

```sh
cd frontend
pnpm exec biome format --write src/
pnpm check
```

All passed. Pipeline test totals were 40 for items 1–4, 46 for item 5, and 61
for items 6–7. Final mypy result: no issues in 19 source files. Frontend checks
reported zero errors, warnings or hints. `git diff --check` passed.

Dependency downloads failed because network DNS is unavailable. Existing
installed dependencies were copied from the other local checkout into this
workspace's ignored `.venv` and `node_modules`, with local executable/import
paths corrected. Pipeline checks used `UV_NO_SYNC=1`,
`UV_CACHE_DIR=/private/tmp/boston-followups-uv-cache`, and
`PYTHON_DOTENV_DISABLED=1`. This avoided dependency downloads and dotenv access;
no dependency manifests or lockfiles changed. A fresh dependency installation
was not verified in this sandbox.

Additional verification: four scraper tests passed using
`cd services/open311-scraper && ../../pipeline/.venv/bin/python -m pytest -q`.
Parsed the workflow YAML and compared every pre-existing job and trigger to
HEAD; all are unchanged. A synthetic Astro server-rendering check passed for
alerts with healthy layers, alerts with no layers, older snapshots without
alerts, null health, HTML escaping, and the existing stale-source notice. The
standalone rendering harness initially lacked Astro's compiler options; after
using the options from the installed Astro compile module it passed without
product changes.

## 1. Classifier precision sample tooling

- Retained the implemented `pipeline/src/pipeline/tools/precision_sample.py`
  and `tools/__init__.py` from the partial commit. The manual Typer module joins
  stored positives to the two litter slugs, samples with a seed across
  slug/month/confidence strata, writes the requested CSV columns with blank
  labels/notes, and prints saved HIGH/MEDIUM signal counts. It handles Boston
  date boundaries and refuses to overwrite human labels.
- Extended `pipeline/tests/test_precision_sample.py` to verify redistribution
  when a stratum is exhausted, preservation of scored text including an empty
  closure, BPW signal counts, and absence of bucket output writes. Existing
  moto tests cover eligibility, deduplication, reproducibility, boundaries,
  invalid sample sizes, CSV quoting and CLI use.
- `docs/wiki/data-quality-issues.md` documents the procedure, weighted precision
  calculation and target **≥0.85 before the layer is trusted**. Fixed duplicate
  section numbering: timestamp correction remains §15; validation is §16.
- Verified: all required checks, 40 pipeline tests.
- Left: a human must run the manual audit against an explicitly selected
  read-only snapshot and label/adjudicate it; precision has not been measured.
- Proposed commit: `feat: finish deterministic waste precision audit tooling`.

## 2. Encampment intake finding

- Reviewed the inherited `docs/wiki/encampment-intake-ended-2026.md`, including
  May 27 cutoff, Jan–May monthly counts, cross-code August sample and Other /
  Litter text-scan evidence. Corrected the scraper code attribution: its
  configured code is the legacy colon-delimited code; the UUID audit check is
  described separately.
- `frontend/src/pages/data-quality.astro` retains the migration/Encampments
  subsection and now links directly to the wiki evidence. The inherited
  `docs/wiki/INDEX.md` link remains in place.
- Verified against the supplied September audit facts, migration plan and
  scraper configuration; all required checks, 40 pipeline tests.
- Left: no verified successor intake exists; keep the historical-series caveat.
- Proposed commit: `docs: finish encampment intake finding and site citation`.

## 3. Source-health runbook

- Added `docs/wiki/source-health-runbook.md` and linked it from
  `docs/wiki/INDEX.md`. Covers all source keys and raw inputs, 30-day and
  365-day-shifted baseline windows, rounded ratio thresholds, stale precedence,
  optimistic layer aggregation, nonempty Open311 day dates, discovery baseline
  behavior and responses to each requested failure.
- Explicitly distinguishes current data delivery from coverage or classifier
  precision, and explains why an old health snapshot can still say OK.
- Verified against `health.py`, `run.py` and scraper configuration; all required
  checks, 40 pipeline tests. Item 5 adds the alert-field documentation.
- Left: none beyond the shared Git commit limitation.
- Proposed commit: `docs: add source health operations runbook`.

## 4. Scraper CI tests

- Added the independent `scraper-test` job to `.github/workflows/pr.yml`, using
  Python 3.12, `python -m pip install boto3 pytest` and `python -m pytest -q` in
  `services/open311-scraper`.
- Every existing job and trigger is unchanged, as requested. This includes the
  existing aggregate `all-checks-pass` job: the new scraper result is a separate
  check and was not added to that aggregate's dependency list.
- Verified: YAML parse and structural comparison; four scraper tests; all
  required pipeline/frontend checks, 40 pipeline tests.
- Left: GitHub-hosted execution was not triggered; branch-protection settings
  were not changed.
- Proposed commit: `ci: run Open311 scraper tests on pull requests`.

## 5. Needle migration watch

- `pipeline/src/pipeline/health.py` detects new names matching
  `needle|syringe|sharp` case insensitively, adds the requested notice to both
  `notes` and top-level `alerts`, and always emits an alerts list.
- `frontend/src/lib/types.ts` accepts optional alerts for older snapshots.
  `frontend/src/components/DataFreshness.astro` displays alert notice text even
  when every layer is OK or no layer entries exist.
- `pipeline/tests/test_health.py` covers each signal, mixed case, multiple
  names, nonmatches, empty results, persisted JSON and baseline suppression.
  Updated `docs/wiki/source-health-runbook.md` to explain alerts and their
  one-snapshot lifetime.
- Verified: all required checks, 46 pipeline tests, plus the synthetic Astro
  rendering scenarios described above.
- Left: additions/alerts are snapshot events, not persistent acknowledgments;
  operators should preserve the snapshot and follow the runbook.
- Proposed commit: `feat: surface needle intake migration alerts`.

## 6. Legacy 2011–2014 needle backfill — UNWIRED after verification (Claude, 2026-09-07)

> Real-world check: the four CSVs contain zero needle-related rows (see wiki §17), so the daily-run wiring
> was removed and needles still start in 2015. The reader stays as a tool; urllib needed a redirect fix
> (strip `:443` from the presigned S3 URL) to download at all.


- Added `pipeline/src/pipeline/legacy_csv.py`. Resolves the exact resource URL
  through `package_show?id=311-service-requests`; streams GET responses through
  a CSV reader; retains NEEDLE_TYPES and case-insensitive needle substrings in
  `type`; preserves CKAN-shaped columns, IDs and ZIP strings. Uses urllib's
  redirect handling, not HEAD. Invalid metadata, schema, rows or downloads fail
  rather than producing a successful empty cache.
- Added the four IDs to separate `LEGACY_CSV_RESOURCES` in
  `pipeline/src/pipeline/config.py`, leaving `RESOURCE_IDS` unchanged.
  `pipeline/src/pipeline/run.py` starts needles at 2011 and includes the CSV
  years only for needles. Caches `raw/needles_<year>.json`, including successful
  empty results; normal reruns reuse them, while force still refreshes them.
- Added `pipeline/tests/test_legacy_csv.py`: mocked streams for all four years,
  URL discovery, bounded reads, header normalization, quoted multiline fields,
  filtering, cleaner compatibility, failures, exact moto cache keys, empty
  cache reuse, forced refresh, start-year/dataset isolation, and current-year
  datastore refresh behavior.
- Updated the sharps coverage entry in `CLAUDE.md`. Searching frontend for
  `2015` found only two numerical counts in `src/data/mattress.json`, so no
  frontend copy or data values were changed for this item.
- Verified: all required checks, 61 pipeline tests.
- Left: real downloads and production cache population were intentionally not
  run; they will occur through a normally deployed pipeline when uncached.
- Proposed commit: `feat: backfill historical needle years from cached CSVs`.

## 7. Mattress appointment feed note

- Added exactly two prose lines under Asks in `docs/wiki/mattress-data.md`:
  Firebase project `sanitation-scheduling-prod`, Cloud Function `getAddresses`,
  no public appointment data, and a city CSV request as the only path.
- No application code changed for this item. Rewrote this report.
- Verified: checked the note against the supplied finding; all required checks,
  61 pipeline tests.
- Left: the city CSV request remains an external follow-up; no message was sent.
- Proposed commit: `docs: record mattress appointment export constraint`.

## Final requested Git log

`git log --oneline origin/main..HEAD` (unchanged because Git metadata is read-only):

```text
ae5ed97 wip(codex): item 2 partial — encampment finding in wiki/data-quality + test patches (Codex run hit usage limit)
```
